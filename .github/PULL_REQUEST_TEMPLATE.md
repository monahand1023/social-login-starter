## What this changes



## Checklist
- [ ] `npm test` passes
- [ ] `node --check` passes for any JS I touched
- [ ] No secrets committed (no real `config.js`, no `infra/parameters.sh`, no `.p8`)
- [ ] No real account-specific IDs, domains, or secrets added — this is a public **template**, keep it generic
- [ ] Docs updated if behavior changed
